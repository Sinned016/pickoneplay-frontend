export default function Settings() {
  // STart with creating backend functions.
  // Fetch user, if no user online then go back to homepage.
  // IF user is logged in then fetch his info.
  return (
    <div className="max-w-7xl mx-auto mt-8 mb-16 px-4 sm:px-6 lg:px-8">
      <div>Settings</div>
    </div>
  );
}
